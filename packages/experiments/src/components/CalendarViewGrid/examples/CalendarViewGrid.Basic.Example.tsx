import * as React from 'react';
import { CalendarViewGrid } from '../../CalendarViewGrid';

export class CalendarViewGridBasicExample extends React.Component {
  public render(): JSX.Element {
    return (
      <CalendarViewGrid />
    );
  }
}
